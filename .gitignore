# ── Environment & Secrets ──
.env
.env.*
*.env

# ── GCP / Firebase Credentials ──
*.json
.agent/*.json
!package.json
!package-lock.json
!firestore.indexes.json
!firebase.json

# ── Agent sensitive ──
.agent/.rule.md

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# ── Raw data (large PDFs) ──
.raw_data/

# ── IDE ──
.vscode/
.idea/

# ── OS ──
Thumbs.db
.DS_Store

# ── Data artifacts (large, rebuild from pipeline) ──
data/images/
data/images_*/
data/extracted/
data/extracted_*/
data/db/
data/pages/
# NOTE: data/kg/ is tracked — contains KG graphml required for service

# ── Large archives ──
*.zip

# ── Test / utility scripts (root level) ──
test_*.py
test_*.png
query_*.py
update_docker.py
extract_images_*.py
test_vis.js
old_dashboard.js

# ── Firebase local cache ──
.firebase/

# ── Scratch (temp files) ──
scratch/
